/**
 * Store a set of words in an array and display the contents both forward and backward.
 */

var words = ['This', 'is', 'a', 'array', 'of', 'string'];

// forward
console.log(words.join());

// backward
console.log(words.reverse().join());
